const browser: string= "Chrome";
const testCount: number = 25;
const testPassed: boolean = true;
const environment: string = "QA";

console.log(browser, testCount, testPassed, environment);


//type annotation
let browserName: String;
let retryCount: number;
let loginSuccessful: boolean;

browserName = "Chrome";
retryCount = 3;
loginSuccessful = true;

console.log(browserName, retryCount, loginSuccessful);


